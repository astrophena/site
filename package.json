{
  "name": "site",
  "private": true,
  "devDependencies": {
    "prettier": "^2.6.1",
    "prettier-plugin-go-template": "^0.0.11"
  },
  "scripts": {
    "check": "gofmt -d . && prettier --check .",
    "fmt": "gofmt -w . && prettier --write ."
  }
}
